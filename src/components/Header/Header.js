import React from 'react';

import classes from "./Header.module.css";



const Header = () => {
    return (
        <main className={`${classes.wrap} width flex`}>
            Header
        </main>
    );
};

export {Header};