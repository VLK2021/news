import React from 'react';
import classes from "./Footer.module.css";


const Footer = () => {

    return (
        <main className={`${classes.wrap} width flex`}>
            Footer
        </main>
    );
};

export {Footer};